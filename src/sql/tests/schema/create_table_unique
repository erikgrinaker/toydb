Query: CREATE TABLE name (id INTEGER PRIMARY KEY, value STRING UNIQUE)
Effect: CreateTable { name: "name" }

Storage:
CREATE TABLE name (
  id INTEGER PRIMARY KEY,
  value STRING DEFAULT NULL UNIQUE
)
