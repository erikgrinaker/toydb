Query: DROP TABLE source
Effect: DropTable { name: "source" }

Storage:
CREATE TABLE self (
  id INTEGER PRIMARY KEY,
  self_id INTEGER DEFAULT NULL REFERENCES self
)

CREATE TABLE target (
  id INTEGER PRIMARY KEY
)
