Query: INSERT INTO TeSt (ID, Name) VALUES (1, 'a')
Effect: Create { count: 1 }

Storage:
CREATE TABLE other (
  id INTEGER PRIMARY KEY
)

CREATE TABLE test (
  id INTEGER PRIMARY KEY DEFAULT 0,
  name STRING DEFAULT NULL,
  value INTEGER DEFAULT NULL
)
[Integer(1), String("a"), Null]
