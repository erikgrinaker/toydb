Query: CREATE TABLE name (id INTEGER PRIMARY KEY, value STRING NOT NULL UNIQUE)
Result: CreateTable { name: "name" }

Storage:
CREATE TABLE name (
  id INTEGER PRIMARY KEY,
  value STRING NOT NULL UNIQUE INDEX
)

Index name.value
